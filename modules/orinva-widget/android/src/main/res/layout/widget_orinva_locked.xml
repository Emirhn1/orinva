<?xml version="1.0" encoding="utf-8"?>
<!--
  Default, privacy-safe state shown whenever the device is locked and the
  user has not explicitly opted in to sensitive content (Part 4 §privacy):
  no behavior name, no slip info, no progress — a generic mark only.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/widget_background"
    android:padding="14dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ORINVA"
        android:textColor="#F4F5F9"
        android:textSize="13sp"
        android:textStyle="bold" />

</LinearLayout>
